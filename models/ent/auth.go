// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"wing/models/ent/auth"
	"wing/models/ent/user"

	"entgo.io/ent/dialect/sql"
)

// Auth is the model entity for the Auth schema.
type Auth struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// LastAuthTime holds the value of the "last_auth_time" field.
	LastAuthTime time.Time `json:"last_auth_time,omitempty"`
	// AuthType holds the value of the "auth_type" field.
	AuthType auth.AuthType `json:"auth_type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AuthQuery when eager-loading is set.
	Edges               AuthEdges `json:"edges"`
	user_auth_histories *int
}

// AuthEdges holds the relations/edges for other nodes in the graph.
type AuthEdges struct {
	// BelongTo holds the value of the belong_to edge.
	BelongTo *User `json:"belong_to,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BelongToOrErr returns the BelongTo value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AuthEdges) BelongToOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.BelongTo == nil {
			// The edge belong_to was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.BelongTo, nil
	}
	return nil, &NotLoadedError{edge: "belong_to"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Auth) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case auth.FieldID:
			values[i] = new(sql.NullInt64)
		case auth.FieldAuthType:
			values[i] = new(sql.NullString)
		case auth.FieldLastAuthTime:
			values[i] = new(sql.NullTime)
		case auth.ForeignKeys[0]: // user_auth_histories
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Auth", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Auth fields.
func (a *Auth) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case auth.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int(value.Int64)
		case auth.FieldLastAuthTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_auth_time", values[i])
			} else if value.Valid {
				a.LastAuthTime = value.Time
			}
		case auth.FieldAuthType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field auth_type", values[i])
			} else if value.Valid {
				a.AuthType = auth.AuthType(value.String)
			}
		case auth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_auth_histories", value)
			} else if value.Valid {
				a.user_auth_histories = new(int)
				*a.user_auth_histories = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryBelongTo queries the "belong_to" edge of the Auth entity.
func (a *Auth) QueryBelongTo() *UserQuery {
	return (&AuthClient{config: a.config}).QueryBelongTo(a)
}

// Update returns a builder for updating this Auth.
// Note that you need to call Auth.Unwrap() before calling this method if this Auth
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Auth) Update() *AuthUpdateOne {
	return (&AuthClient{config: a.config}).UpdateOne(a)
}

// Unwrap unwraps the Auth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Auth) Unwrap() *Auth {
	tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Auth is not a transactional entity")
	}
	a.config.driver = tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Auth) String() string {
	var builder strings.Builder
	builder.WriteString("Auth(")
	builder.WriteString(fmt.Sprintf("id=%v", a.ID))
	builder.WriteString(", last_auth_time=")
	builder.WriteString(a.LastAuthTime.Format(time.ANSIC))
	builder.WriteString(", auth_type=")
	builder.WriteString(fmt.Sprintf("%v", a.AuthType))
	builder.WriteByte(')')
	return builder.String()
}

// Auths is a parsable slice of Auth.
type Auths []*Auth

func (a Auths) config(cfg config) {
	for _i := range a {
		a[_i].config = cfg
	}
}
